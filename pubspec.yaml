name: state_management
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  restaurant_app_bloc:
    path: package/restaurant_app_bloc
  restaurant_app_mobx:
    path: package/restaurant_app_mobx
  restaurant_app_provider:
    path: package/restaurant_app_provider
  restaurant_app_redux:
    path: package/restaurant_app_redux
  restaurant_app_riverpod:
    path: package/restaurant_app_riverpod
  restaurant_domain:
    path: package/restaurant_domain
  restaurant_ui:
    path: package/restaurant_ui

dev_dependencies:
  build_runner: ^2.3.3
  flutter_test:
    sdk: flutter
  freezed: ^2.2.0
  surf_lint_rules: ^1.5.0

flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

