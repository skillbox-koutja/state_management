import 'package:flutter/material.dart';

class RestaurantLoader extends StatelessWidget {
  const RestaurantLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
